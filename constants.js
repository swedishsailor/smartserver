/* Node js environment variables */
const PORT = 5000;

/* Databse variables */
const DB_HOST = 'localhost';
const DB_USER = 'root';
const DB_DATABASE = 'smartserver';
const DB_PASSWORD = '';

module.exports={
    PORT,
    DB_HOST,
    DB_USER,
    DB_DATABASE,
    DB_PASSWORD
}